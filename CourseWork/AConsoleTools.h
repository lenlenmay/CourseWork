#pragma once
#include <iostream>
#include <regex>
#include <string>
#include <fstream>
#include <codecvt>
#include <locale>

class AConsoleTools {

public:
	void Clear();
	bool ValidationOfInput(std::string str);
	
	
};